/* ── Couleurs sémantiques ─────────────────────────────────── */

export type Color =
  | "primary"
  | "accent"
  | "neutral"
  | "info"
  | "success"
  | "warning"
  | "danger";

/* ── Variantes ────────────────────────────────────────────── */

export type Variant =
  | "solid"
  | "soft"
  | "ghost"
  | "outline";

/* ── Tailles ──────────────────────────────────────────────── */

export type Size =
  | "xs"
  | "sm"
  | "md"
  | "lg";

/* ── Alignements ──────────────────────────────────────────── */

export type Align =
  | "left"
  | "center"
  | "right";